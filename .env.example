# 凭证加密主密钥（任意非空字符串，内部经 SHA-256 派生 AES-256 密钥）
# 不设置时会在数据目录自动生成 .secret.key 并持久化
WARMUP_ENCRYPTION_KEY=

# 服务端口（默认 8080）
PORT=8080

# 数据目录（SQLite + 密钥文件，默认 ./data）
DATA_DIR=./data

# 内置登录（公网部署建议开启；AUTH_PASSWORD 留空 = 免认证模式）
AUTH_USERNAME=admin
AUTH_PASSWORD=
